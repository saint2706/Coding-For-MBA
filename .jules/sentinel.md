## 2024-05-18 - [HIGH] Fix vulnerable dependencies\n**Vulnerability:** DOMPurify CUSTOM_ELEMENT_HANDLING bypasses afterSanitizeElements for allowed custom elements, and js-yaml YAML merge-key chains can force quadratic CPU consumption.\n**Learning:** Found critical DOMPurify vulnerability that can allow XSS through bypasses in custom element handling. \n**Prevention:** Need to keep security-sensitive packages like dompurify updated via `npm audit`.
