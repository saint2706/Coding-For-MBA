- Refactored useProgressStore to derive and manage `completedSet` (a `Set<number>`) directly within the Zustand store instead of repeatedly instantiating it with `new Set()` across multiple components via `useMemo`. This optimization mitigates O(N*M) recalculation bottlenecks across the UI when `completedLessons` updates.
