- Added `type="button"` to AiStudyPanel buttons (Quick Actions, Clear Chat, Flashcard Generate/Regenerate, Tabs) to prevent default form submissions.\n- Improved flashcard keyboard accessibility by adding `role="button"`, `tabIndex={0}`, and an `onKeyDown` handler for Space/Enter toggling.\n- Added `title` attribute to TableOfContents toggle button for better screen reader and tooltip context.\n- Added `title` attribute to Sidebar close button.
