
- Updated hallucinated Action version pins to latest actual versions (checkout@v4, upload-artifact@v4, etc)
- Optimized Node caching in composite actions to properly cache npm globally rather than local node_modules
