# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local
*.tsbuildinfo
public/sitemap.xml

# Locally-installed Claude Code skills — do NOT commit. Only the
# repo-native settings.json, hooks/, and CLAUDE.md are tracked.
.claude/skills/

# Graphify knowledge graph — local dev aid, not used by CI or the site build.
graphify-out/

# Agent platform symlink directories (auto-generated by npx skills)
.adal/
.agent/

# Claude Code agent worktrees (temporary, auto-generated)
.claude/worktrees/

# Lock files (project uses pnpm)
package-lock.json
yarn.lock

# Editor directories and files
.vscode
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright artifacts
.playwright-artifacts/
*.png
playwright-report/
test-results/
actionlint

# Environment variables
.env
.env.*
!.env.example

# Coverage
coverage/

# Installed agent skills (local only)
.agents/
skills-lock.json
