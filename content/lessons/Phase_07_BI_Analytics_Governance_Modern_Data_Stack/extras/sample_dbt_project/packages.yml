# dbt-utils supplies the dbt_utils.accepted_range and
# dbt_utils.unique_combination_of_columns tests used in this project's
# schema.yml files (see Day 84B Exercise 3).
#
# Install with: dbt deps
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
